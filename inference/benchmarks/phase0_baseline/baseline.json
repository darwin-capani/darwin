{
  "about": "Phase 0 baseline for the performance campaign, measured on the LIVE deployed install (M1 Pro, 17 GB, 2026-07-29). Every later claim in this campaign must diff against this file. Read-only measurements plus one lightweight socket round-trip each; no benchmark loops (see the hazard note).",
  "hazard_note": "Generating load on this machine CRASHES the inference server. Two concurrent Core ML conversions and, separately, one op=generate each triggered a Metal command-buffer timeout (kIOGPUCommandBufferCallbackErrorTimeout, exit -6, launchd restart). Five such timeouts in this log, one of them timestamped 2026-07-28 17:17 which PREDATES this session's changes. Mechanism is memory pressure, see below. CONSEQUENCE: benchmark-heavy phases must not run against the live install.",
  "memory": {
    "why_this_is_first": "This reordered the whole campaign. Shaving milliseconds off IPC is meaningless while 93% of the inference server's writable memory is swapped out.",
    "machine_total_gb": 17,
    "swap_used_gb_at_measurement": 12.8,
    "inference_phys_footprint_mb": 5352,
    "inference_phys_footprint_peak_mb": 7694,
    "inference_metal_unified_gb": 4.3,
    "inference_writable_swapped_out_pct": 93,
    "ane_peak_mb": 142,
    "honesty": "DARWIN is NOT the only pressure. Top RSS at measurement time was Comet (1.6 GB) and Claude helpers (~1.7 GB combined), plus this session's own subagents and cargo builds. DARWIN's share is the 5.35 GB footprint above.",
    "ATTRIBUTION_CORRECTION": "Re-measured after killing this session's leftover subagent processes: swap went UP (12.8 -> 13.4 GB) while DARWIN's footprint held steady at 5208 MB / peak 6979 MB. So the swap pressure is substantially THIS SESSION'S OWN TOOLING (Claude helpers ~1.9 GB plus concurrent audit agents), not DARWIN. HONEST CONSEQUENCE: 'the machine is swapping' is NOT on its own a justification for reclaiming memory from DARWIN - 5.2 GB for a 4B LLM plus STT, TTS and a retrieval stack is not obviously wrong on a 17 GB machine. Memory work here has to be justified as GENUINE WASTE (the spec-decode draft model was: a measured loss AND unreachable), not as relief for pressure an outside process is causing."
  },
  "startup": {
    "CORRECTION": "An earlier draft of this file said TTS dominates startup at 37.3 s of 63 s. That was measured on a CONTENDED boot - the Core ML fast-graph rebuild was converting in the background during it, and its MIL progress bars are interleaved in that very log line. On a clean boot TTS preload is 4.7 s and the TTS WEIGHT load is 0.3-0.5 s. The real dominator is the KV prompt-cache prefills.",
    "total_seconds_wall": 63,
    "clean_boot_stages_seconds": {
      "stt_whisper_small": 1.4,
      "llm_total": 19.6,
      "llm_weights_only": 4.5,
      "llm_persona_prompt_cache_prefill": 15.1,
      "classifier_total": 18.0,
      "coreml_embedder": 13.2,
      "coreml_reranker": 3.9,
      "tts_total": 4.7,
      "tts_weights_only": 0.5,
      "learned_vad": 0,
      "openers_bank": 0.56,
      "draft_model": 2.2
    },
    "finding": "The two KV prompt-cache prefills (persona 2063 tokens, classifier 869 tokens) are ~33 s of the ~63 s. TTS weights are 0.5 s, which makes TTS a CHEAP parking candidate rather than a startup problem. The draft model's 2.2 s goes with speculative decoding.",
    "lesson": "Measure on an UNCONTENDED boot. A background conversion inflated one stage 8x and I recorded it as the headline finding."
  },
  "ipc_overhead": {
    "why": "The ANE work made the graph fast enough that framing now dominates short ops.",
    "embed_short_graph_ms": 1.84,
    "embed_short_through_socket_ms": 6.61,
    "embed_long_through_socket_ms": 21.83,
    "rerank_through_socket_ms_per_pair": 1.84,
    "rerank_baseline_ms_per_pair": 10.88,
    "finding": "A short embed costs 1.84 ms at the graph and 6.61 ms over the unix socket with JSON framing - the framing costs more than the model. Rerank shows 5.9x through the wire vs 9.70x at the graph, same dilution."
  },
  "speculative_decoding": {
    "verdict": "REMOVED - it was a measured loss AND unreachable",
    "source": "inference/benchmarks/baseline_m1_pro.json, results.speculative (already committed; this phase only had to read it)",
    "uncached_decode_tps_off": 60.87,
    "uncached_decode_tps_on": 44.45,
    "ratio": 0.73,
    "cached_path_reachable": false,
    "cached_path_reason": "mlx_lm rejects a draft_model together with a prompt_cache (IndexError); DARWIN's persona and classifier paths are KV-cached, so the production path always ran normal decode",
    "cost_removed_mb": 335,
    "note": "It shipped ON with the draft checkpoint pre-downloaded by the installer, so a 16 GB machine that swaps was holding a 335 MB model the generate path could never use."
  },
  "next": "Memory work first (idle model parking, TTS residency given the opener bank, and whether the GPU-timeout crash class is a memory-pressure symptom), then re-measure before touching IPC or models.",
  "kv_prompt_caches": {
    "computed_from": "Qwen3-4B-Instruct-2507-4bit config.json: 36 layers, 8 kv heads, head_dim 128, fp16 K+V",
    "bytes_per_token_kb": 144.0,
    "persona_2063_tokens_mb": 304.2,
    "classifier_869_tokens_mb": 128.1,
    "both_mb": 432.3,
    "prefill_cost_seconds": "~33 of the ~63 s startup",
    "capability": "mlx_lm 0.31.3 exposes save_prompt_cache / load_prompt_cache / trim_prompt_cache, so these COULD be persisted to disk and loaded instead of re-prefilled each boot",
    "unresolved": "persisting saves STARTUP, not memory (the cache is resident either way). Its real safety argument is that it removes ~33 s of GPU prefill at boot - the most fragile moment, and where the Metal command-buffer timeouts occur. A stale cache silently paired with a changed persona would be a correctness hazard, so it would need a strict fingerprint (model id + quant + prompt hash + mlx_lm version)."
  },
  "confirmed_waste_found": {
    "method": "73-agent audit over resident models, caches and startup work; every finding double-refuted",
    "1_prefill_materialized_891MB_of_discarded_logits": {
      "what": "Both KV prompt-cache prefills ran the FULL model and mx.eval'd the logits, forcing the tied lm_head projection of every prefix position out to vocab_size 151,936.",
      "persona_mb": 626.9,
      "classifier_mb": 264.1,
      "total_mb": 891.0,
      "vs_hidden_states_actually_wanted_mb": 15.0,
      "ratio": "59x",
      "fix": "evaluate [c.state for c in cache] instead - mlx_lm's own prefill pattern. MLX is lazy, so the projection never enters the graph.",
      "proven": "miniature model with the same shape: peak 470.4 MB with eval(logits) vs 159.3 MB with eval(cache), a 311.2 MB saving exactly equal to the logits tensor. Cache is bit-identical either way.",
      "why_it_matters": "the transient landed during boot, which is both the peak-memory moment (7694 MB peak vs 5352 MB steady) and where the Metal command-buffer timeouts occur"
    },
    "2_live_config_still_had_speculative_on": {
      "what": "the repo fix was committed but the LIVE ~/Library/Application Support/DARWIN/config/darwin.toml still carried speculative=true + the draft model, so 335 MB and 2.2 s were still being paid at every boot",
      "resolution": "lands on the next redeploy; verify install.sh overwrites the deployed config rather than preserving it"
    },
    "3_installer_copied_2.5GB_of_benchmark_scratch": {
      "what": "scratch-bench/ is gitignored with zero tracked files and is never read by the runtime, but was not in EXCLUDE_DIRS, so every ./install.sh --yes rsynced 2.5 GB into the install home - almost all of it an ane-probe/ tree with its OWN hf-cache holding a SECOND full copy of the 4B checkpoint",
      "fix": "added to EXCLUDE_DIRS"
    },
    "4_rejected_checkpoints_still_on_disk": {
      "what": "7.32 GiB of candidate models that were downloaded, LOST their eval, and were never cleaned up - TTS candidates rejected by the RTF gate (csm-1b-8bit, orpheus variants, snac vocoder), classifier candidates rejected by the accuracy gate (Qwen3-0.6B, Qwen2.5-0.5B, Llama-3.2-1B), and whisper-large-v3-turbo rejected by the STT comparison",
      "status": "REPORTED ONLY - deleting user files is destructive and was not done without asking"
    }
  },
  "after_phase0_fixes": {
    "measured": "2026-07-29 boot, machine load ~14 (a runaway browser was holding ~2.7 cores; the BEFORE column was taken under comparable contention)",
    "stages_seconds": {
      "stt": 1.8,
      "llm_total": 16.7,
      "llm_weights": 3.2,
      "persona_prefill": 7.4,
      "classifier_total": 3.0,
      "classifier_prefill": 3.0,
      "coreml_embedder": 14.8,
      "coreml_reranker": 6.9,
      "tts": 5.3,
      "draft_model": 0
    },
    "before_after": {
      "classifier_total": "18.0 -> 3.0 s (6.0x)",
      "persona_prefill": "15.1 -> 7.4 s (2.0x)",
      "llm_total": "19.6 -> 16.7 s",
      "draft_model": "2.2 s + 335 MB -> gone",
      "total_startup": "~63 -> ~49 s (about 22% faster)"
    },
    "honest_cost": {
      "coreml_embedder": "13.2 -> 14.8 s and reranker 3.9 -> 6.9 s. This is the ANE fast path loading TWO graphs instead of one - a real startup cost paid for a 10.7x/9.70x per-predict win, and it belongs in the ledger.",
      "net": "the startup saving is real but smaller than the prefill numbers alone suggest, because the Core ML stages grew"
    },
    "what_did_it": "removing the discarded vocab projection from all three prefills (891 MB at boot plus ~1.2 GB per large consolidate), and dropping the speculative draft model that was both a measured 27% decode loss and unreachable on the KV-cached path"
  },
  "phase1_ipc_premise": {
    "the_claim_i_made": "a short embed costs 6.61 ms over the wire vs 1.84 ms at the graph, so ~4.8 ms is IPC overhead worth optimizing (connection pooling + binary framing)",
    "what_was_wrong_with_it": [
      "The 6.61 ms was measured with a FRESH socket per call. I assumed the daemon did the same. It does not - daemon/src/inference.rs caches `conn` and ensure_connected() only connects when it is None. So connection pooling was already there and could not be the win.",
      "Re-measured properly: POOLED p50 8.48 ms vs FRESH p50 8.65 ms - statistically the same. Unix-socket connect is not the cost.",
      "The remaining figure is not trustworthy either. p50 8.48 ms with min 4.03 and p90 11.78 is contention variance, not a fixed cost, and it was taken at machine load ~14 with a runaway browser holding ~2.7 cores. The 1.84 ms graph number it is being differenced against was measured on a quiet machine."
    ],
    "what_can_be_said": "best-case overhead is about 2.2 ms (min 4.03 minus the 1.84 graph cost), part of which is tokenization - real work, not overhead. That is a far smaller prize than 4.8 ms.",
    "verdict": "DEFER, not a NO-GO. The IPC premise cannot be validated on this machine in its current state; the measurement is dominated by contention rather than by the thing the optimization would remove. Re-measure on a quiet machine before building anything.",
    "meta": "This is the THIRD measurement this session corrupted by machine contention (after the 37.3 s TTS figure and the 43 s startup stage). Every remaining campaign phase - retrieval tuning, model-refresh evals - is measurement-dependent, so the machine's load is the blocking issue for the campaign, not any code.",
    "RESOLVED_ON_A_QUIET_MACHINE": {
      "context": "the user closed the runaway browser; load fell 43 -> 8 and swap 13.4 -> 3.46 GB, so these numbers are finally trustworthy",
      "short_embed_p50_ms": 4.08,
      "long_embed_p50_ms": 19.33,
      "long_embed_graph_cost_ms": 19.65,
      "the_decisive_observation": "the LONG embed costs 19.33 ms on the wire against a 19.65 ms graph cost - wire overhead is within noise of zero. A fixed multi-millisecond IPC cost would have made it ~25 ms.",
      "breakdown_of_the_4.08_ms_short_request": {
        "bare_round_trip_full_dispatch_no_model_work_ms": 0.215,
        "json_encode_decode_of_one_384_float_vector_ms": 0.105,
        "everything_else_tokenize_predict_gpu_lock_asyncio_ms": "~3.86"
      },
      "verdict": "NO-GO, not a defer. The entire IPC + dispatch + framing path is 0.22 ms of a 4.08 ms request (5%). Binary framing would target the 0.105 ms of JSON; connection pooling already exists in daemon/src/inference.rs. There is no prize here.",
      "what_this_retires": "the 'IPC fast path' half of Phase 1. The remaining Phase 1 items (TTS first-audio streaming, an ANE-native STT evaluation) are untouched by this and still stand on their own."
    }
  },
  "clean_boot_2026_07_29_quiet_machine": {
    "conditions": "load ~4, swap 3.46 GB, runaway browser closed - the first genuinely uncontended measurement of this session",
    "stages_seconds": {
      "stt": 0.8,
      "llm_weights": 1.3,
      "persona_prefill": 7.3,
      "llm_total": 11.0,
      "classifier_prefill": 3.1,
      "classifier_total": 3.5,
      "coreml_embedder": 8.0,
      "coreml_reranker": 3.7,
      "tts_weights": 0.2,
      "tts_total": 3.1,
      "openers": 1.0,
      "draft_model": 0
    },
    "total_seconds": 31.1,
    "vs_baseline_63s": "about 2x faster",
    "ATTRIBUTION_HONESTY": "the 63 s baseline was itself taken with the browser running, so this 2x is my fixes AND the machine getting quiet, and I cannot cleanly separate them from these two numbers alone. What IS separately attributable: the draft model went 2.2 s -> 0 by config, and the classifier stage went 18.0 -> 4.2 s while contention was STILL present, so that 4.3x is the logits-elision fix rather than the browser.",
    "peak_footprint_mb": 6203,
    "peak_vs_baseline_7694": "-1491 MB",
    "steady_footprint_mb": 5094,
    "largest_remaining_startup_items": {
      "coreml_embedder_s": 8.0,
      "persona_prefill_s": 7.3,
      "note": "the Core ML embedder cost includes loading TWO graphs (the ANE fast path); the persona prefill is 2063 tokens of KV recomputed from scratch on every boot and mlx_lm 0.31.3 can persist it"
    }
  },
  "steady_state_boot_with_persisted_caches": {
    "measured": "2026-07-29, quiet machine, second and later boots (the first boot after deploy still prefills and saves)",
    "stages_seconds": {
      "stt": 0.7,
      "llm_total": 3.5,
      "persona_restore": 0.001,
      "classifier_total": 0.0,
      "classifier_restore": 0.0,
      "coreml_embedder": 7.7,
      "coreml_reranker": 3.8,
      "tts": 2.7,
      "openers": 0.54
    },
    "total_seconds": 18.9,
    "trajectory": {
      "original_baseline_s": 63,
      "after_logits_and_specdecode_s": 31.1,
      "with_persisted_caches_s": 18.9,
      "overall": "3.3x"
    },
    "prefill_before_after": {
      "persona": "7742 ms -> 1 ms",
      "classifier": "3254 ms -> 0 ms"
    },
    "correctness_verified_not_assumed": {
      "method": "captured classify labels for 8 real utterances against the RESTORED cache, then moved the cache aside, forced a fresh prefill, and re-ran the same 8",
      "result": "8/8 identical",
      "also": "the classifier survived 4 consecutive turns, which exercises the trim_prompt_cache reuse path a restored cache could plausibly have broken"
    },
    "disk_cost_mb": 412,
    "NEW_DOMINANT_ITEM": "Core ML is now 11.5 s of the 18.9 s (embedder 7.7 + reranker 3.8) - 61% of startup. Part of that is the ANE fast path deliberately loading TWO graphs per model. It is the obvious next target, but note coremltools CONVERSION is not thread-safe (that bug cost a deploy tonight); whether LOADING can be parallelized is unmeasured and must not be assumed."
  },
  "compiled_model_cache": {
    "the_finding": "ct.models.MLModel(<dir>.mlpackage) pays a FULL Core ML program specialization on every load. It is not the mlpackage->mlmodelc compilation (that is ~104 ms); it is the specialization, which Core ML caches per COMPILED model - and MLModel compiles the package into a throwaway temp .mlmodelc each time, so it never hits that cache.",
    "per_graph_load_ms_from_mlpackage": {
      "emb_b1": 4606,
      "emb_b1_s128": 2914,
      "rerank_b1": 2258,
      "rerank_b1_s128": 1465
    },
    "validate_predicts_ms_total": 77,
    "note": "11.24 s of the 11.5 s Core ML startup was MLModel loads; the validate-predicts were 77 ms",
    "fix": "persist the .mlmodelc beside the .mlpackage and load it with CompiledMLModel, compiling once on first use; fall back to the .mlpackage on any problem",
    "measured_after": {
      "both_models_across_fresh_processes_s": 2.5,
      "before_s": 11.24,
      "speedup": "4.5x"
    },
    "HONEST_LIMIT": "an isolated single-graph test showed 4400 ms -> 65 ms (68x) and I initially expected ~0.3 s total. The settled cross-process figure is 2.5 s, not 0.3 s: the largest graph (the 64 MB 512 embedder) does not stay warm in Core ML's cross-process specialization cache, and repeatedly loading four graphs plus stray test copies caused visible eviction churn. The 8.7 s saving is real; the 11x I first inferred from one graph in isolation was not.",
    "disk_cost_mb": 214,
    "disk_note": "the .mlmodelc is the same size as its .mlpackage (64/64/43/43 MB). Both are kept so a corrupt compiled model falls back to a slow load rather than a 40 s reconversion.",
    "process_note": "the temp destination must ALSO end in .mlmodelc - compile_model validates the extension. That shipped wrong once and only the fallback hid it, which is the SECOND extension-validating API to catch me in this session after mx.save_safetensors."
  },
  "boot_after_compiled_model_cache": {
    "measured": "2026-07-29 on the deployed install, quiet machine, all four .mlmodelc warm",
    "stages_seconds": {
      "stt": 0.9,
      "llm_total": 4.2,
      "persona_restore": 0.007,
      "classifier_total": 0.0,
      "coreml_embedder": 0.5,
      "coreml_reranker": 0.1,
      "tts": 2.7,
      "openers": 0.56,
      "learned_vad": 0
    },
    "total_seconds": 8.96,
    "trajectory": {
      "original": 63,
      "after_logits_and_specdecode": 31.1,
      "after_persisted_prompt_caches": 18.9,
      "after_compiled_model_cache": 8.96,
      "overall": "7.0x"
    },
    "coreml_before_after": "11.5 s -> 0.6 s on the real boot",
    "BETTER_THAN_MY_OWN_ESTIMATE": "my synthetic harness measured 2.5 s for both models and I recorded that as the expected result. The real boot is 0.6 s. The harness was pessimistic because it rebuilt objects in a loop and I had stray test copies of the compiled models competing for Core ML's specialization cache at the time. Reporting the real boot as the number, and the discrepancy as a caution about trusting a harness over the thing itself.",
    "coreml_cache_disk_mb": 429,
    "largest_remaining": {
      "llm_weights_s": 4.2,
      "tts_s": 2.7,
      "stt_s": 0.9,
      "openers_s": 0.56,
      "tts_note": "TTS weights load in 0.2 s, so ~2.5 s of that stage is the throwaway 'Ready.' synthesis that compiles Metal kernels. The opener bank is synthesized immediately afterwards and has to run anyway - so the throwaway may be duplicate work. UNVERIFIED; needs checking before touching."
    }
  },
  "tts_warmup_hypothesis": {
    "the_hypothesis": "TTS preload is 2.7 s of which weights are only 0.2 s, so ~2.5 s is a throwaway 'Ready.' synthesis compiling Metal kernels - and since the opener bank synthesizes 5 lines immediately afterwards and has to run anyway, the throwaway looked like duplicate work worth ~2.5 s.",
    "verdict": "WRONG - measured NO-GO",
    "why": "the opener bank really does synthesize (it deletes stale WAVs and calls _synthesize_to_wav per line), but it is FAST precisely because the throwaway already compiled the kernels. Measured directly: weights 133 ms, FIRST synthesis 2386 ms, and the 5 openers take 562 ms total (~112 ms each) only because they run after it. Removing the throwaway moves the 2.4 s compile onto the first opener; it does not remove it. Net saving ~0.1 s.",
    "the_only_real_lever_and_why_it_is_shut": "the 2.4 s is MLX's Metal kernel JIT. mx.metal exposes clear_cache / get_cache_memory / set_cache_limit - all IN-PROCESS - and there is no persistent kernel cache API, so the compile cannot be carried across boots with what is exposed today.",
    "lesson": "this is the third assumption of this shape I have had to retract by measuring (after the IPC fast path and the 2.5 s Core ML estimate). Reasoning about where time goes has been wrong more often than not; measuring has been right every time."
  },
  "startup_work_status": {
    "conclusion": "startup optimization has reached diminishing returns at 8.96 s, 7.0x off the baseline",
    "remaining_and_why_each_is_closed": {
      "llm_weights_4.2s": "loading a 4B checkpoint; only a smaller model would move it, which is Phase 3 evaluation territory rather than a fix",
      "tts_2.4s": "Metal kernel JIT, no persistent cache API - measured NO-GO above",
      "stt_0.9s": "small",
      "openers_0.56s": "5 real syntheses, already warm-path",
      "coreml_0.6s": "was 11.5 s; done"
    },
    "where_the_remaining_campaign_value_is": "Phase 2 (retrieval quality) and Phase 3 (model refresh) - being SMARTER rather than faster. Startup is no longer where the wins are."
  }
}